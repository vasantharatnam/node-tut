// npm - global command, comes with node
//npm --version

// local dependency - use it only in this particular project
// npm i <packageName>

// global dependency - use it in any project
// npm install -g <packageName>
// sudo npm install -g <packageName> (mac os)

// package.json - json file that contains meta data about a project
//manual approach (create a package.json in the root of the project , create properties in there)
// npm init (step by step, press enter to skip)
// npm init -y (everything is default